import 'package:flutter/material.dart';

/// Couleurs de l'application
class AppColors {
  AppColors._(); // Private constructor

  static const Color primary = Color(0XFF005B96);
  static const Color background = Color(0XFFE5E5E5);
  static const Color card = Color.fromARGB(255, 243, 242, 242);
  
  // Couleurs additionnelles utilisées dans l'app
  static const Color blueLight = Color.fromARGB(255, 44, 149, 254);
  static const Color blueMedium = Color.fromARGB(255, 68, 171, 255);
  static const Color blueDark = Color(0xFF255BC1);
}

